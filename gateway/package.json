{
  "name": "@valuemaxx/gateway",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "valuemaxx capture gateway — an observe-only reverse proxy for LLM providers",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240614.0",
    "typescript": "^5.5.0",
    "vitest": "^4.0.0",
    "wrangler": "^4.0.0"
  }
}
