[project]
name = "atm-outcomes"
version = "0.0.0"
description = "AI Margin Intelligence — outcomes logic package (skeleton; built in G2)."
requires-python = ">=3.11"
license = "Apache-2.0"
dependencies = [
    "atm-core",
    "atm-capabilities",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/atm_outcomes"]

[tool.uv.sources]
atm-core = { workspace = true }
atm-capabilities = { workspace = true }
